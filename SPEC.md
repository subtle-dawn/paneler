# Paneler 仕様書

## 1. 概要

### 1.1 アプリ名

**Paneler**

### 1.2 目的

漫画制作における「文字ネーム」から、ページごとのコマ枠を自動生成するWebアプリ。

ユーザーが各コマについて、

* ページ
* 感情の大きさ
* コマの大きさ
* 役割
* 形
* カメラ
* 内容

を入力すると、「コマの大きさ」と「形」をもとにページ内へコマを自動配置する。

AIによる構図生成や作画は行わない。

Panelerの担当範囲は、

**文字ネーム → コマ割り**

までとする。

人物の配置、顔の大きさ、具体的な構図などは、生成されたコマ枠をもとにユーザーがネームを描く段階で決定する。

# 2. 基本コンセプト

漫画制作工程を以下のように分ける。

1. プロット
2. 文字ネーム
3. Panelerによるコマ起こし
4. ネーム
5. 下描き
6. ペン入れ・仕上げ

Panelerでは、文字ネームの段階ですでに決定されている「コマの大きさ」「コマの形」を機械的にページレイアウトへ変換する。

AIによる判断は原則使用しない。

# 3. 対応環境

## 3.1 形式

Webアプリ。

PCでの利用を主用途とする。

スマートフォン・タブレットでも最低限閲覧・編集可能なレスポンシブUIとする。

# 4. データ構造

## 4.1 作品

```ts
type Project = {
  id: string;
  title: string;
  readingDirection: "rtl" | "ltr";
  pages: Page[];
};
```

## 4.2 ページ

```ts
type Page = {
  id: string;
  pageNumber: number;
  panels: Panel[];
};
```

## 4.3 コマ

```ts
type Panel = {
  id: string;

  emotionSize: "small" | "medium" | "large" | null;
  panelSize: "small" | "medium" | "large";
  role: string;
  shape: "vertical" | "square" | "horizontal";
  camera: string;
  content: string;

  order: number;
};
```

初期段階では `role` と `camera` は自由入力でもよい。

「感情の大きさ」はコマの自動配置には使用せず、文字ネーム上の情報として保持する。

# 5. 文字ネーム入力

## 5.1 基本項目

各コマについて以下を入力できる。

| 項目  | 内容                |
| --- | ----------------- |
| ページ | 何ページ目か            |
| 感情  | 感情の大きさ            |
| 大きさ | コマの大きさ            |
| 役割  | 場、時など             |
| 形   | 縦、正、横             |
| カメラ | 引、俯、横など           |
| 内容  | セリフ、モノローグ、動作、背景など |

## 5.2 感情

選択肢：

* 小
* 中
* 大
* なし

## 5.3 コマの大きさ

選択肢：

* 小
* 中
* 大

## 5.4 コマの形

選択肢：

* 縦
* 正
* 横

「正」は正方形に近い形を意味する。

# 6. 文字ネーム編集UI

スプレッドシートに近い表形式で編集する。

例：

| Page | 感情 | 大きさ | 役割  | 形 | カメラ | 内容                   |
| ---: | -- | --- | --- | - | --- | -------------------- |
|    1 | 小  | 小   | 場・時 | 縦 | 引   | 家の外観                 |
|    1 | 小  | 大   | 場   | 正 | 俯   | 家の内観。妹がキッチンで弁当を作っている |
|    1 | 小  | 中   | -   | 正 | 俯   | 妹モノローグ……             |

各行＝1コマとする。

行は、

* 追加
* 削除
* 複製
* 並び替え

が可能。

# 7. Excel / CSVインポート

文字ネームを表計算ソフトで作成しているユーザー向けに、ExcelまたはCSVから読み込めるようにする。

最低限以下の列名に対応する。

* ページ
* 感情
* 大きさ
* 役割
* 形
* カメラ
* 内容

列名については日本語・英語双方への対応を将来的に検討する。

インポート後はPaneler内部のデータ形式へ変換する。

不明な値が存在する場合はエラーで全体を拒否するのではなく、該当セルを警告表示してユーザーが修正できるようにする。

# 8. 読み方向

作品ごとに読み方向を設定する。

## Manga

**Right to Left**

日本漫画向け。

基本的な視線誘導：

右上 → 左上 → 右下 → 左下

## Comics

**Left to Right**

英語圏のコミック向け。

基本的な視線誘導：

左上 → 右上 → 左下 → 右下

設定UI：

```text
Reading Direction

● Right to Left (Manga)
○ Left to Right (Comics)
```

初期値は Right to Left (Manga) とする。

# 9. コマ自動生成

## 9.1 基本原則

コマ割りはAIではなくルールベースで生成する。

主に以下の2項目を使用する。

* コマの大きさ
* コマの形

その他の文字ネーム情報は配置判断には原則使用しない。

# 10. コマサイズ

ページを仮想的なグリッドとして扱う。

初期実装では6列グリッドを基本とする。

概念上：

```css
.page {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
}
```

サイズの基本値：

```text
小 = 2列
中 = 3列
大 = 6列
```

つまり、

```text
小 + 小 + 小
中 + 中
大
```

で1段を構成できる。

# 11. コマ形状

基本的な縦横比：

```text
縦：2 / 3
正：1 / 1
横：3 / 2
```

CSSでは `aspect-ratio` を利用してよい。

例：

```css
.panel.vertical {
  aspect-ratio: 2 / 3;
}

.panel.square {
  aspect-ratio: 1 / 1;
}

.panel.horizontal {
  aspect-ratio: 3 / 2;
}
```

ただしページ全体の高さに収める必要があるため、最終的にはページ内のコマ数に応じて縮小してよい。

# 12. 配置アルゴリズム

## 12.1 基本

文字ネームの順番を絶対に維持する。

コマの順序が入れ替わるような `grid-auto-flow: dense` の使用は避ける。

## 12.2 RTL

Right to Leftの場合、各段を右側から配置する。

例：

```text
┌─────┬─────┐
│  2  │  1  │
├─────┼─────┤
│  4  │  3  │
└─────┴─────┘
```

## 12.3 LTR

Left to Rightの場合、各段を左側から配置する。

```text
┌─────┬─────┐
│  1  │  2  │
├─────┼─────┤
│  3  │  4  │
└─────┴─────┘
```

## 12.4 行生成

現在の行に次のコマが入る場合、そのまま追加する。

入らない場合は新しい行を作成する。

例：

```text
中 + 中
→ 同じ行

小 + 小 + 小
→ 同じ行

中 + 小
→ 同じ行

中 + 中 + 小
→ 最後の小は次の行
```

「大」は原則として1行を占有する。

# 13. ページ高さへのフィット

漫画原稿のページは固定比率で表示する。

ページ内の全コマが収まるよう、生成後に各行の高さを調整する。

コマ数が多い場合は全体を縮小する。

コマ数が極端に多く、実用的な大きさでは収まらない場合は、

「このページはコマ数が多すぎる可能性があります」

などの警告を表示する。

自動的に次ページへコマを移動させない。

ページ構成は作者の指定を優先する。

# 14. コマ内表示

生成されたコマには文字ネーム情報を表示する。

例：

```text
┌──────────────────┐
│ 3                │
│                  │
│ ［中・正］        │
│ ［俯］            │
│                  │
│ 妹モノローグ      │
│ 「お母さんと……」  │
│ 包丁で卵焼きを切る│
└──────────────────┘
```

最低限、

* コマ番号
* 大きさ
* 形
* カメラ
* 内容

を表示する。

表示項目は将来的にON/OFF可能にしてもよい。

# 15. プレビュー

編集画面では、

**文字ネーム / ページプレビュー**

を同時に確認できるUIが望ましい。

PC：

```text
┌─────────────────┬────────────┐
│                 │            │
│   文字ネーム     │ ページ     │
│   編集           │ プレビュー │
│                 │            │
└─────────────────┴────────────┘
```

文字ネームを変更すると、プレビューへ即時反映する。

# 16. 手動調整

自動生成後、コマを手動調整できるようにする。

MVPでは最低限、

* コマの大きさ変更
* コマの形変更
* コマ順変更

に対応する。

ドラッグによる自由変形・自由配置は後から実装してよい。

重要なのは、

**自動生成結果を絶対視せず、人間が簡単に修正できること**

とする。

# 17. ページ一覧

作品全体をページ単位で確認できる。

```text
Page 1    Page 2    Page 3
┌────┐    ┌────┐    ┌────┐
│    │    │    │    │    │
│    │    │    │    │    │
└────┘    └────┘    └────┘
```

ページをクリックすると編集画面へ移動する。

# 18. 保存

MVPではブラウザのLocalStorageまたはIndexedDBで保存してよい。

アカウント機能・サーバー・DBは必須としない。

作品データをJSONとして、

* エクスポート
* インポート

できるようにする。

これによりブラウザデータ消失時にもバックアップできる。

# 19. 出力

最低限、生成されたページを画像として保存できることを目標とする。

候補：

* PNG
* JPEG

将来的には、

* PDF
* Clip Studio Paint等へ持ち込むための画像出力

を検討する。

出力時はUIを除外し、ページ部分のみを書き出す。

# 20. UI言語

初期実装は日本語。

将来的な英語対応を考慮し、UI文字列はコードへ直接埋め込まず、i18n化しやすい構造にする。

主要用語：

```text
コマ = Panel
コマ割り = Panel Layout / Paneling
コマ起こし = Generate Panels
文字ネーム = Text Storyboard / Text Name
読み方向 = Reading Direction
```

# 21. MVPで実装するもの

最初のバージョンでは以下に限定する。

1. 新規作品作成
2. 作品タイトル設定
3. RTL / LTR設定
4. 表形式で文字ネーム入力
5. 行追加・削除・並び替え
6. ページ指定
7. 感情：小・中・大
8. コマサイズ：小・中・大
9. コマ形状：縦・正・横
10. 役割入力
11. カメラ入力
12. 内容入力
13. コマの自動配置
14. ページプレビュー
15. LocalStorage等への自動保存
16. JSONインポート / エクスポート
17. Excel / CSVインポート

# 22. MVPでは実装しないもの

以下は初期実装の対象外。

* AIによるコマ割り
* AIによる構図提案
* AI画像生成
* キャラクター配置
* 顔サイズ指定
* 棒人間生成
* 吹き出し自動配置
* セリフ解析
* アカウント
* クラウド同期
* 複数人共同編集
* SNS機能

必要になった場合のみ後から追加する。

# 23. 技術構成案

シンプルなクライアントサイドWebアプリとする。

推奨：

```text
Vite
React
TypeScript
CSS
```

Excel読み込みが必要な場合のみ対応ライブラリを追加する。

状態管理ライブラリはMVPでは必須ではない。

バックエンドは使用しない。

# 24. 設計上の優先順位

優先順位は以下。

1. 読み順が壊れない
2. 文字ネームから素早くコマを生成できる
3. 操作が簡単
4. 自動生成結果を容易に修正できる
5. 作品データを失わない
6. 将来的に機能追加しやすい

# 25. Panelerの基本思想

Panelerは「漫画を描いてくれるアプリ」ではない。

漫画家・漫画制作者が文字ネームですでに決めた演出を、実際のページ上のコマ枠へ素早く変換するための補助ツールである。

そのため、アプリ側が作者の意図を推測することよりも、

**作者が指定した情報を忠実かつ機械的にレイアウトへ変換すること**

を優先する。
